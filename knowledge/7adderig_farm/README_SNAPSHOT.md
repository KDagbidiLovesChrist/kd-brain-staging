# 7 adderig farm · PHONE SNAPSHOT (read-only mirror)

**This is a lightweight, READ-ONLY mirror** of the house fit-out project so it's visible from the phone
(Claude Code web), where the heavy project folder isn't synced.

- **Source of truth (edit here):** `C:\Users\Dell\Documents\7 adderig farm\` on the **laptop**.
  Heavy assets (videos, 3D renders, the `house.html` walkthrough) stay there, too big for the lean brain.
- **What's mirrored here:** the markdown specs (`CLAUDE.md`, `MASTER_PROMPT.md`, `decisions.md`,
  `LAYOUT_TRUTH.md`, `MEDIA_INVENTORY.md`, `INSPIRATION_LOOK.md`, `rooms\*.md`) + floorplan + key
  reference images. Enough to **read, plan, cost, and design** from the phone.
- **On the phone you CAN:** read the project, plan rooms, work the cost list, draft specs.
- **On the phone you CAN'T:** build the 3D walkthrough or generate renders (those need laptop tools/keys).
- **Rule to avoid drift:** edits to the project happen on the **laptop** (source of truth). This snapshot
  is refreshed FROM the laptop. If you change specs on the phone, tell Claude to carry them back to the
  laptop `Documents` folder on the next laptop session.

*Snapshot taken 2026-06-20. To refresh: re-copy the `.md` + floorplan from the laptop `Documents` folder.*
