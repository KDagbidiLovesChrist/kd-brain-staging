# The "Fable 5 OS / Jarvis" gap map — KD brain vs @theromanknox's 5-step blueprint (2026-07-04)
> King sent the 8-slide carousel (@theromanknox, skool.com/knox): "my brain, my tool, aide, Logos system — I want it set up like this; I feel I'm heading this direction." Verdict: **he IS — ~70% is already built, and the missing 30% is literally the V2 vision already on file** (`plans\can-u-get-guardian-purring-stearns.md`). This file is the honest per-component comparison + the build order.

## The comparison (his 5 steps vs the KD brain today)
| # | Blueprint piece | KD brain status |
|---|---|---|
| 1 | **The Build** — a personal Jarvis command center | 🟡 PARTIAL. `/aide` IS the Jarvis role (any door, God-first, one next move) — but text-driven, no voice loop, no standing screen. |
| 2 | **The Brain** — skill architecture: branches → SKILL.md folders, load-on-demand | ✅ **DONE, deeper than his.** 54 commands + 57 plugin skills (Logic layer · 57th = karpathy-llm-wiki + 7 video/web skills, installed 2026-07-04), 5 sub-brains branching from God, MANIFEST + tier ledger. Nothing to copy here. |
| 3 | **The Memory** — Obsidian vault, markdown graph, reports land back as md | ✅ **DONE.** The `.claude` brain IS an Obsidian vault (stone #4), graph view hall-coloured, `/save`+`/learn` route everything back to markdown. |
| 4 | **The Voice** — 100% local: faster-whisper STT → route (regex/local/Haiku) → **Kokoro** TTS | ❌ **MISSING.** King's voice today = phone voice-typing in; ElevenLabs PVC out (paid, for content not command). Local stack is FREE + fits Rule #12: **Ollama already installed** on the Dell; add faster-whisper + Kokoro. Keys stay local — matches the keys-off-cloud rule. |
| 5 | **The Face** — one HUD: metrics + voice orb + command deck (V.A.U.L.T.) | 🟡 PARTIAL. We have the DCIM (kd-brain-dcim.vercel.app, God the living centre), 2D map, 3D dashboard, Notion mirror — **views, not a live command center** (no live metrics feed, no clickable command deck, no voice I/O). V2's "3D 24/7 brain" = exactly this. |
| + | **The Handoff** — one repo, clone + .env + setup.sh, **reskin per client + SELL** | 🟡 PLANNED = V2's sellable **"King AI OS" template** + `/os_builder` (€1k-5k setup + retainer). His carousel is market proof people BUY this (his skool business is built on it). |

## What of today's CLI-skills wave feeds this
The 28 skills installed 2026-07-04 slot straight in: `frontend-design`/`ui-ux-pro-max` suite → build the HUD beautifully; `webapp-testing` → QA it; Remotion/Higgsfield → the content that shows it off.

## Build order (respects MONEY MODE — each step earns or sells)
1. **NOW (free, laptop):** wire local voice — faster-whisper (STT) + Kokoro (TTS) + Ollama routing, glued to `/aide`. King literally talks to the Aide, it talks back. €0. Also becomes a killer @30Kingdavid video ("I built Jarvis with Claude — free").
2. **NEXT:** upgrade the DCIM/3D dashboard into the **KD V.A.U.L.T.** — live command deck (the Logic skills as buttons), metrics (subs/€/queue from the engines), voice orb. UI UX Pro Max + frontend-design skills do the design; webapp-testing QAs it.
3. **THEN (the money move):** bundle as the sellable **King AI OS** template via `/os_builder` — clone, .env, setup, reskin per client. The KD brain itself is the demo. Price per the os_builder tier (€1k-5k + monthly).
Gate: each step storyboarded/planned → King's yes → build (Rules #17/#18/#21). No new spend anywhere; ElevenLabs stays content-only.

## Guard (CREED)
The blueprint's "Jarvis" framing stays subordinate: the KD system's centre is **God, not the tool** (`_ops/CREED.md`). The HUD keeps the DCIM's God-centred design — the command center serves the mission, never replaces the Source.
